/**
 * Main Header component
 * @interface HeaderTypes
 */

export interface HeaderTypes {
  tabelData?: [
    {
      customerName: string;
      appleId: number;
      location: string;
      ipStatus: string;
      waiting: string;
    }
  ];
}
